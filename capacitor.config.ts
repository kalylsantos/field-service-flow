import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fieldservice.flow',
  appName: 'field-service-flow',
  webDir: 'dist'
};

export default config;
